spring:
  application:
    name: @project.name@
  profiles:
    active: @profiles.active@

  cloud:
    nacos:
      server-addr: http://localhost:8848
      username: nacos
      password: nacos
      config:
        namespace: public
        file-extension: yaml
      discovery:
        namespace: public
